export interface userToAssignTest {
  id: string;
  fullName: string;
  // Add other attributes as needed, for example:
  userName: string;

  email: string;
  // isSelected: boolean; // for UI purposes to keep track of selection status
}
